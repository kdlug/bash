#/bin/bash

# ARRAYS
arr=( zero one two three )

echo ${arr[*]} # list all elements of the array
echo ${arr[0]} # list the 1st element of the array
echo ${arr[1]}
echo ${#arr[0]} # print out the link of the first element
echo ${#arr[*]} # print out the size of the array
